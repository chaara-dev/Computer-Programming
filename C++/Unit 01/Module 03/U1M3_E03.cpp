#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int exponent = 2;

    cout << pow(2, exponent) << endl; cout << pow(3, exponent) << endl;
    cout << pow(4, exponent) << endl; cout << pow(5, exponent) << endl;
    cout << pow(6, exponent) << endl; cout << pow(7, exponent) << endl;
    cout << pow(8, exponent) << endl; cout << pow(9, exponent) << endl;
    cout << pow(10, exponent) << endl; cout << pow(11, exponent) << endl;
    cout << pow(12, exponent) << endl;
    return 0;
}