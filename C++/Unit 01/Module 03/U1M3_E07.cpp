#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << "The square (number times itself) of 2 times " << endl;
    cout << "the number you get when you add 2 and 1 together is:" << endl;
    cout << pow(((1 + 2) * 2), 2);
    return 0;
}