#include <iostream>
#include <cmath>
#include <string>

using namespace std;

int main() {
    cout << "9.03 being used in a .ciel() function -> "; cout << ceil(9.03) << endl;
    cout << "9.03 being used in a .floor() function -> "; cout << floor(9.03);

    return 0;
}